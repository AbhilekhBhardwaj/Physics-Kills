{
  "name": "physics-kills",
  "description": "Physics-Kills",
  "private": true,
  "scripts": {
    "dev": "npm start --prefix frontend",
    "start": "npm start --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
